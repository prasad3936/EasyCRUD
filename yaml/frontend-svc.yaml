kind: Service
apiVersion: v1
metadata:
  name: frontend-svc
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8088
      targetPort: 80
  selector:
    app: frontend-app
  type: NodePort
