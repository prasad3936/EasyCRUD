apiVersion: v1
kind: Pod
metadata:
  name: backend-pod
  labels:
    app: backend-app
spec:
  containers:
    - name: backend-app
      image:
      ports:
        - containerPort: 8080
  template:
    spec:
      containers:
        - image: ghcr.io/prasad3936/easyback:2d4de1e0111ea149bda5beca01721cce09fc849e
